# Исправления после ревью перед переносом на PHP

- [x] Запрет тега `pos` только из `?` и `*`
- [ ] Отложено: сравнение в MariaDB (ѐ=е, ѝ=и в `unicode_ci`) — ждём версию сервера
- [x] Шаг 1. Импорт: строка только с `diplomatic` не выбрасывается; отрицательные числа — понятная ошибка, а не `IntegrityError`
- [x] Шаг 2. Фронтенд, контекст: ключ по находке, сброс при новом поиске, ошибка ≠ «нет предложений»
- [ ] Шаг 3. Фронтенд, список текстов: следовать за адресом; не писать старую страницу при ошибке; один тост вместо двух
- [ ] Шаг 4. Фронтенд, форма поиска: Reset снимает `partial`; показывать сообщение сервера
- [ ] Шаг 5. Решить про `/api/v1/speakers/` (сайт не использует, в PHP его нет)
- [ ] Шаг 6. В `ЗАГРУЗКА.md`: позиции подсветки в PHP считать через `mb_*`
