# План: бэкенд под новый формат данных

Временный трекинг-файл. Удалить, когда работа закончена.

## Решения (утверждены)
- Метаданные: поля прямо на `Text` + `Text.authors = M2M(Speaker)`
- Чтение .xlsx через `openpyxl` (новая зависимость)
- Поиск по UD: базовое отношение захватывает подтипы (`nsubj` → `nsubj:pass`)
- `text_id` — слаг, общий ключ метаданных и аннотаций

## Этапы
- [x] 1. Модели + миграция (слой БД) — применена 0003, старые данные очищены
- [ ] 2. Пакет `core/utils/`, views худеют до HTTP, сериализаторы под новые поля
- [ ] 3. Три команды импорта: `import_texts`, `import_speakers`, `upload_transcription`
- [ ] 4. Проверка на реальных файлах: счётчики в БД + живые ответы API

## Шаг 5 (сделан): валидация, транзакция и логгер во всех трёх импортах

## Открытые вопросы к Oli
- Файл `..._Speakers_New.csv` содержит копию документов, данных по спикерам нет
- Из метаданных пропала колонка `Short Description`
- В `Variety` латинская `j` вместо кириллической `ј` (`Диjалектен`)

## Предложение по моделированию корпуса — отдельно, кода не пишем
См. сообщение с планом: JSONB для `pos_ext`, `head` как FK, денормализованный
текст предложения, tsvector/pg_trgm, разделение type/token, версии аннотаций.
