export default defineAppConfig({
  ui: {
    // 'terracotta' is defined in assets/css/styles.css; 'stone' is Tailwind's
    // warm grey, which sits better on the paper background than slate.
    colors: {
      primary: 'terracotta',
      neutral: 'stone',
    },
  },
})
